# This will become the crontab file. Place any crons that need performing in here.
# This needs to "link up" with how files are added to the conainer from the dockerfile.

# Run the thread_executor every minute.
* * * * * /usr/bin/php /var/www/downloader/project/scripts/task_executor/main.php
